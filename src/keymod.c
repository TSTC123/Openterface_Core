#include "keymod.h"

/*
 * Compatibility translation unit retained so existing integrations that
 * compile src/keymod.c continue to find a stable entry point file.
 * The implementation now lives in keymod_packets.c, keymod_hid.c,
 * and keymod_parser.c.
 */
